EXPERIMENT_NAME: goal_tag_dragpoint_text_fix_1.0_uncond_random10

ROLLOUT:
  UNCOND: True

DATASET:
  SKIP_CACHE_CHECK: True
  CACHE_MAP: False
  SOURCE:
    ROLLOUT: ['waymo_train']
  DATA_LIST:
    MODE: scene
    ROLLOUT: waymo_train_16scenes.txt

PROMPT:
  CONDITION:
    TYPES: ['llm_text_OneText']
    SAMPLE_MODE:
      ROLLOUT: 'none'
    RANDOM_SAMPLE:
      ROLLOUT: False
